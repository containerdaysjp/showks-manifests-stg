---
# Source: showks-canvas/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: showks-canvas-inductor-temp
  namespace: showks
  labels:
    app: showks-canvas-inductor-temp
    class: showks-canvas
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: app-port
  selector:
    app: showks-canvas-inductor-temp

---
# Source: showks-canvas/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: showks-canvas-inductor-temp
  namespace: showks
  labels:
    app: showks-canvas-inductor-temp
    class: showks-canvas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showks-canvas-inductor-temp
  template:
    metadata:
      labels:
        app: showks-canvas-inductor-temp
        class: showks-canvas
    spec:
      containers:
        - name: showks-canvas-inductor-temp
          image: gcr.io/ipc-containerdays/showks-canvas-inductor:stg-d2600b08fcb7fa6d2380d15026ce7ac8dbd0590b
          ports:
            - name: app-port
              containerPort: 8080
              protocol: TCP

---
# Source: showks-canvas/templates/ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: showks-canvas-inductor-temp
  namespace: showks
  labels:
    class: showks-canvas
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: showks-canvas-inductor.stg.showks.containerdays.jp
    http:
      paths:
      - path: /
        backend:
          serviceName: showks-canvas-inductor-temp
          servicePort: 8080

