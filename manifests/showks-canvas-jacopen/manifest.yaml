---
# Source: showks-canvas/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: showks-canvas-jacopen-temp
  namespace: showks
  labels:
    app: showks-canvas-jacopen-temp
    class: showks-canvas
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: app-port
  selector:
    app: showks-canvas-jacopen-temp

---
# Source: showks-canvas/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: showks-canvas-jacopen-temp
  namespace: showks
  labels:
    app: showks-canvas-jacopen-temp
    class: showks-canvas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showks-canvas-jacopen-temp
  template:
    metadata:
      labels:
        app: showks-canvas-jacopen-temp
        class: showks-canvas
    spec:
      containers:
        - name: showks-canvas-jacopen-temp
          image: gcr.io/ipc-containerdays/showks-canvas-jacopen:stg-f72cee755d6a762da79b7f475663fda157f458c2
          ports:
            - name: app-port
              containerPort: 8080
              protocol: TCP

---
# Source: showks-canvas/templates/ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: showks-canvas-jacopen-temp
  namespace: showks
  labels:
    class: showks-canvas
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: showks-canvas-jacopen.stg.showks.containerdays.jp
    http:
      paths:
      - path: /
        backend:
          serviceName: showks-canvas-jacopen-temp
          servicePort: 8080

